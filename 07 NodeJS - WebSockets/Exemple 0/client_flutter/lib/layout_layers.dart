import 'package:flutter/cupertino.dart';

class LayoutLayers extends StatelessWidget {
  const LayoutLayers({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Layout Layers',
        style: TextStyle(fontSize: 16.0),
      ),
    );
  }
}
